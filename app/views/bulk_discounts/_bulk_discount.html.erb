<div id="bulk_discount_<%= bulk_discount.id %>">
  <p><%= link_to "Discount Info", merchant_bulk_discount_path(@merchant, bulk_discount) if local_assigns[:links]%></p>
  <p>Discount Name: <%= bulk_discount.name %></p>
  <p>Discount: <%= bulk_discount.discount %>%</p>
  <p>Threshold: <%= bulk_discount.threshold %></p>
  <p><%= link_to "Delete Discount", merchant_bulk_discount_path(@merchant, bulk_discount), method: :delete if local_assigns[:links]%></p>
  <br>
</div>